name: grpc_kv
description: Dart gRPC KV Server and Client with mTLS support
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  grpc: ^4.0.1
  protobuf: ^3.1.0
  args: ^2.4.0

dev_dependencies:
  build_runner: ^2.4.0
  protoc_plugin: ^21.1.1
